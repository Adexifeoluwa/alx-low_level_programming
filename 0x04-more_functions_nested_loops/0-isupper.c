#include <stdio.h>
#include "main.h"

/*
* Check if it is UpperCase or Not
*check using ASCII Value
*ASCII Value for (A - Z) 65 - 90)
*covert every charater givern to its ASCII Value
*compare tge ACSII Value if it falls within the range
*if it does, then return 1, otherwise return 0
*/

int _isupper(int c)
{
	if (c >= 65 && is c <= 90)
	{
		return (1);
	}
	return (0);
}
